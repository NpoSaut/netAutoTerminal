<UserControl x:Class="FirmwarePacker.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:FirmwarePacker.ViewModels"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:triggerActions="clr-namespace:FirmwarePacker.TriggerActions"
             xmlns:views="clr-namespace:FirmwarePacker.Views"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance viewModels:MainViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <views:VersionView DataContext="{Binding Version}" />
        <Button Grid.Row="1" Margin="20, 2" Content="Сохранить" IsDefault="True" Command="{Binding SaveCommand}" />
    </Grid>


    <i:Interaction.Triggers>
        <i:EventTrigger SourceObject="{Binding Path=SaveFileRequest}" EventName="Raised">
            <triggerActions:ShowSaveFileDialog />
        </i:EventTrigger>
    </i:Interaction.Triggers>
</UserControl>