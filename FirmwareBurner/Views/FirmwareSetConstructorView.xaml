<UserControl x:Class="FirmwareBurner.Views.FirmwareSetConstructorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:FirmwareBurner.ViewModels"
             xmlns:property="clr-namespace:FirmwareBurner.ViewModels.Property"
             mc:Ignorable="d" d:DesignWidth="300"
             d:DataContext="{x:Static viewModels:SampleData.FirmwareSetConstructor}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="SharedResources.xaml"/>
                <ResourceDictionary Source="FirmwareSelectors/FirmwareSelectorsDataTemplates.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <ListBox ItemsSource="{Binding Components}" SelectedIndex="0">
        <ListBox.ItemTemplate>
            <DataTemplate DataType="{x:Type property:ValidateableFirmwareSetComponentViewModel}">
                <ContentControl Style="{StaticResource ValidateableControlStyle}" HorizontalAlignment="Stretch">
                    <HeaderedContentControl Header="{Binding Value.ModuleName}">
                        <ContentPresenter x:Name="ContentPresenter" Content="{Binding Value.FirmwareSelector}" />
                    </HeaderedContentControl>
                </ContentControl>
                <DataTemplate.Triggers>
                    <DataTrigger
                        Binding="{Binding Mode=OneWay, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListBoxItem}}, Path=IsSelected}"
                        Value="False">
                        <Setter TargetName="ContentPresenter" Property="Visibility" Value="Collapsed" />
                    </DataTrigger>
                </DataTemplate.Triggers>
            </DataTemplate>
        </ListBox.ItemTemplate>
        <ListBox.ItemContainerStyle>
            <Style TargetType="{x:Type ListBoxItem}">
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            </Style>
        </ListBox.ItemContainerStyle>
        <ListBox.Template>
            <ControlTemplate>
                <ItemsPresenter />
            </ControlTemplate>
        </ListBox.Template>
    </ListBox>
</UserControl>