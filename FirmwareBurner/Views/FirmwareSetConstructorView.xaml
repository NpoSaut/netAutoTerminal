<UserControl x:Class="FirmwareBurner.Views.FirmwareSetConstructorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:FirmwareBurner.ViewModels"
             mc:Ignorable="d" d:DesignWidth="300"
             d:DataContext="{x:Static viewModels:SampleData.FirmwareSetConstructor}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="FirmwareSelectors/FirmwareSelectorsDataTemplates.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <TabControl ItemsSource="{Binding Components}" DisplayMemberPath="ModuleName" SelectedIndex="0">
        <TabControl.ContentTemplate>
            <DataTemplate DataType="{x:Type viewModels:FirmwareSetComponentViewModel}">
                <ContentPresenter Content="{Binding FirmwareSelector}" />
            </DataTemplate>
        </TabControl.ContentTemplate>
    </TabControl>
</UserControl>