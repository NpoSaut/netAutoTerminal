<UserControl x:Class="FirmwareBurner.Views.FirmwareSetConstructorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:FirmwareBurner.ViewModels"
             mc:Ignorable="d" d:DesignWidth="300"
             d:DataContext="{x:Static viewModels:SampleData.FirmwareSetConstructor}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="FirmwareSelectors/FirmwareSelectorsDataTemplates.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <ListBox ItemsSource="{Binding Components}" SelectedIndex="0">
        <ListBox.ItemTemplate>
            <DataTemplate DataType="viewModels:FirmwareSetComponentViewModel">
                <HeaderedContentControl Header="{Binding ModuleName}">
                    <ContentPresenter x:Name="ContentPresenter" Content="{Binding FirmwareSelector}" />
                </HeaderedContentControl>
                <DataTemplate.Triggers>
                    <DataTrigger
                        Binding="{Binding Mode=OneWay, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListBoxItem}}, Path=IsSelected}"
                        Value="False">
                        <Setter TargetName="ContentPresenter" Property="Visibility" Value="Collapsed" />
                    </DataTrigger>
                </DataTemplate.Triggers>
            </DataTemplate>
        </ListBox.ItemTemplate>
        <ListBox.Template>
            <ControlTemplate>
                <ItemsPresenter />
            </ControlTemplate>
        </ListBox.Template>
    </ListBox>
</UserControl>