<UserControl x:Class="FirmwareBurner.Views.ProjectManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:FirmwareBurner.Views"
             xmlns:viewModels="clr-namespace:FirmwareBurner.ViewModels"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Rectangle Grid.ColumnSpan="2" Grid.RowSpan="3" Name="BurningDataShadow" Fill="#3547A0E8" />
        <Polygon Grid.Column="2" Grid.RowSpan="3" Points="2,4 0,0 0,8" Stretch="Fill"
                     Height="{Binding ElementName=BurningDataShadow, Path=ActualHeight}">
            <Polygon.Fill>
                <LinearGradientBrush EndPoint="1,0.5" StartPoint="0,0.5">
                    <GradientStop Color="#3547A0E8" Offset="0" />
                    <GradientStop Color="#A247A0E8" Offset="1" />
                </LinearGradientBrush>
            </Polygon.Fill>
        </Polygon>

        <Label Grid.Column="0" Grid.Row="0" Target="{Binding ElementName=SerialNumberEdit}"
                   Content="Серийный номер блока" />
        <Label Grid.Column="0" Grid.Row="1" Target="{Binding ElementName=AssemblyDateEdit}"
                   Content="Дата изготовления" />

        <TextBox Grid.Column="1" Grid.Row="0" Name="SerialNumberEdit"
                     Text="{Binding Path=Project.BlockDetails.SerialNumber, UpdateSourceTrigger=PropertyChanged}" />
        <DatePicker Grid.Column="1" Grid.Row="1" Name="AssemblyDateEdit" BorderBrush="{x:Null}"
                        SelectedDate="{Binding Path=Project.BlockDetails.AssemblyDate}" />

        <views:FirmwareSetConstructorView Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2"
                                              DataContext="{Binding Project.FirmwareSetConstructor}"
                                              d:DataContext="{x:Static viewModels:SampleData.FirmwareSetConstructor}" />

        <ListBox Grid.Column="2" Grid.RowSpan="3" VerticalAlignment="Center" HorizontalAlignment="Center"
                     ItemsSource="{Binding Burning.ChannelSelector.Channels}"
                     SelectedItem="{Binding Burning.ChannelSelector.SelectedChannel}"
                     Style="{StaticResource ChannelSelectorStyle}" />

        <Grid Grid.Column="3" Grid.RowSpan="4" IsEnabled="{Binding Path=Validator.IsValid}">
            <Button Width="60" Height="60" Style="{StaticResource CircleButton}"
                        Command="{Binding Path=Burning.DefaultVariant.BurnCommand}"
                        Content="Начать">
                <Button.ContextMenu>
                    <ContextMenu Placement="Right" ItemsSource="{Binding Burning.BurningVariants}">
                        <ContextMenu.ItemContainerStyle>
                            <Style TargetType="MenuItem">
                                <Setter Property="Header" Value="{Binding Name}" />
                                <Setter Property="Command" Value="{Binding BurnCommand}" />
                            </Style>
                        </ContextMenu.ItemContainerStyle>
                    </ContextMenu>
                </Button.ContextMenu>
            </Button>
            <i:Interaction.Triggers>
                <i:EventTrigger SourceObject="{Binding Burning}" EventName="" />
            </i:Interaction.Triggers>
        </Grid>

        <ItemsControl Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Visibility="Collapsed"
                          ItemsSource="{Binding Path=Validator.ValidationErrors}" />

    </Grid>
</UserControl>
