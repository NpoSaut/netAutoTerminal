<UserControl x:Class="FirmwareBurner.Views.FirmwareSelectors.RepositoryFirmwareSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:FirmwareBurner.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="300"
             d:DataContext="{x:Static viewModels:SampleData.RepositoryFirmwareSelector}">
    <Grid>
        <ListView ItemsSource="{Binding Packages}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Версия" DisplayMemberBinding="{Binding Path=Information.FirmwareVersion}" />
                    <GridViewColumn Header="Метка" DisplayMemberBinding="{Binding Path=Information.FirmwareVersionLabel}" />
                    <GridViewColumn Header="Дата релиза" DisplayMemberBinding="{Binding Path=Information.ReleaseDate}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
