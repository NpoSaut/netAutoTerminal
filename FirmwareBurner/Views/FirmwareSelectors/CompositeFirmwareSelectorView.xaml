<UserControl x:Class="FirmwareBurner.Views.FirmwareSelectors.CompositeFirmwareSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:FirmwareBurner.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="100" d:DesignWidth="250"
             d:DataContext="{x:Static viewModels:SampleData.CompositeFirmwareSelector}">
    <TabControl ItemsSource="{Binding Children}" SelectedItem="{Binding SelectedSelector}" Padding="4">
        <TabControl.ItemTemplate>
            <DataTemplate>
                <TextBlock Text="{Binding Name}" />
            </DataTemplate>
        </TabControl.ItemTemplate>
    </TabControl>
</UserControl>