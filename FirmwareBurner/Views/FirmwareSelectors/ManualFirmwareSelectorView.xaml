<UserControl x:Class="FirmwareBurner.Views.FirmwareSelectors.ManualFirmwareSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:firmwareSources="clr-namespace:FirmwareBurner.ViewModels.FirmwareSources"
             mc:Ignorable="d" Width="200"
             d:DataContext="{d:DesignInstance firmwareSources:ManualFirmwareSelectorViewModel}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../SharedResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <StackPanel>
        <TextBlock>
            <Hyperlink Command="{Binding OpenPackageCommand}">
                <Run Text="{Binding FirmwarePath, Mode=OneWay}" />
            </Hyperlink>
        </TextBlock>
        <ContentPresenter Content="{Binding SelectedVersion}" />
    </StackPanel>
</UserControl>